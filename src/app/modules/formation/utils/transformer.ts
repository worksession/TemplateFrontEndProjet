import {FormationModel} from '../model/formation.model';
import {Programme, Experience, Groupe} from '../model/formation.model';

export class Transformer {

  private static formationModel:FormationModel = new FormationModel();

  public static WSToObject(data) {

    
    return data;
  }

}

